<div class="round-show max-w-3xl mx-auto px-4">

  <h1 class="text-2xl font-bold mb-6">ラウンド詳細</h1>

  <!-- 日付・ゴルフ場 -->
  <section class="bg-white p-4 rounded shadow mb-6">
    <p class="text-lg font-semibold">
      <%= @round.date.strftime("%Y年%m月%d日") %>
    </p>
    <p class="text-gray-700">
      <%= @round.course_name %>
    </p>
  </section>

  <!-- スコア情報 -->
  <section class="bg-white p-4 rounded shadow mb-6">
    <h2 class="text-xl font-semibold mb-2">スコア</h2>

    <% front = @round.scores.find { |s| s.part == "front" } %>
    <% back  = @round.scores.find { |s| s.part == "back" } %>

    <ul class="space-y-1">
      <li>
        前半：
        <%= front&.half_score.presence || "-" %>
        （パット：<%= front&.half_putts.presence || "-" %>）
      </li>
      <li>
        後半：
        <%= back&.half_score.presence || "-" %>
        （パット：<%= back&.half_putts.presence || "-" %>）
      </li>
      <li class="font-semibold mt-2">
        トータル：
        <%= @round.total_score.presence || "-" %>
        （パット：<%= @round.total_putts.presence || "-" %>）
      </li>
    </ul>

    <div style="margin-top: 16px;">
      <%= link_to "ラウンドを編集する",
                  edit_round_path(@round),
                  class: "underline text-gray-800 hover:opacity-70" %>
    </div>
  </section>

  <!-- ランチ情報 -->
  <section class="bg-white p-4 rounded shadow mb-6">
    <h2 class="text-xl font-semibold mb-4">ランチ情報</h2>

    <% if @round.lunch.present? %>
      <% lunch = @round.lunch %>

      <% if lunch.image.attached? %>
        <%= image_tag lunch.image.variant(resize_to_limit: [600, 600]),
              class: "block w-full rounded shadow",
              style: "margin-bottom: 12px;" %>
      <% end %>

      <p>
        コメント：
        <%= lunch.comment.presence || "（コメントはまだありません）" %>
      </p>
    <% else %>
      <p class="text-gray-500">
        ランチ情報はまだ登録されていません。
      </p>
    <% end %>

    <div style="margin-top: 16px;">
  <% if @round.lunch.present? %>
    <%= link_to "ランチを編集する",
          edit_lunch_path(@round.lunch),
          class: "underline font-semibold text-gray-800 hover:opacity-70" %>
  <% else %>
    <%= link_to "ランチを登録する",
          new_lunch_path(round_id: @round.id),
          class: "underline font-semibold text-gray-800 hover:opacity-70" %>
  <% end %>
</div>

  </section>

  <!-- ▼ 操作リンクエリア（間は inline style で固定） -->
  <div style="margin-top: 48px; margin-bottom: 64px;">

    <div style="margin-bottom: 24px;">
      <%= link_to "このラウンドを削除する",
                  round_path(@round),
                  data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" },
                  class: "text-sm text-red-600 underline hover:text-red-800 block" %>
    </div>

    <div>
      <%= link_to "マイページに戻る",
                  root_path,
                  class: "text-sm text-gray-600 underline hover:text-gray-800 block" %>
    </div>

  </div>

</div>
